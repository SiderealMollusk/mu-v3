{
  "name": "logging_stout",
  "version": "0.1.0",
  "description": "Stout domain for logging via NATS",
  "main": "services/index.ts",
  "scripts": {
    "start": "ts-node --transpile-only services/index.ts",
    "dev": "ts-node-dev --poll --respawn --transpile-only --ignore-watch node_modules services/index.ts",
    "build": "tsc",
    "test": "vitest"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "nats": "^2.29.3"
  },
  "devDependencies": {
    "ts-node": "^10.9.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.3.3",
    "vitest": "^1.0.0"
  }
}
